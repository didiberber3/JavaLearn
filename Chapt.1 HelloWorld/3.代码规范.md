1. 类、方法的注释，要以`javadoc`的方式来写

2. 非`javadoc`方式来写的注释往往是给维护者看的

3. 使用tab操作实现缩进默认整体向右侧移动，`shift`+`tab`向左

4. 运算符和=两边习惯各加一个空格

   比如 `2 + 4 * 5 = 22`

5. 源文件使用`utf-8`编码

6. 行宽度不要超过`80`字符

7. 代码编写**次行风格**和**行尾风格**

   1. 每一行的行尾有大括号是**行尾风格**
   2. 推荐**行尾风格**